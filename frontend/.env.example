# Frontend Environment Variables
# Copy this file to .env.local for development or .env for production

# API Configuration
# Development: Use localhost
# Production: Use your deployed backend URL (e.g., https://your-backend-cloudrun-url.run.app)
NEXT_PUBLIC_API_URL=http://localhost:8000

# API Protection Key (must match backend API_KEY)
# Generate with: openssl rand -base64 32
# IMPORTANT: Use the same key in both frontend and backend
NEXT_PUBLIC_API_KEY=your_secure_api_key_here

# Production Notes:
# 1. Replace NEXT_PUBLIC_API_URL with your Cloud Run backend URL
# 2. Ensure NEXT_PUBLIC_API_KEY matches your backend API_KEY exactly
# 3. These variables are embedded in the client bundle - keep API_KEY secure but not secret 